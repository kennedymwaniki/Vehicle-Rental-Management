import Login from "../features/Login/Login";
import RegisterComponent from "../features/Login/Register";
RegisterComponent;
const Hero = () => {
  return (
    <div>
      Hero
      <Login />
      <RegisterComponent />
    </div>
  );
};

export default Hero;
